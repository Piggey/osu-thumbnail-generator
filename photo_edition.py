import cv2
import numpy as np

def editPhoto(filepath, artist, title, player):
    canvas = np.zeros((1280, 720, 3), np.uint8)
    pass

    # cv2.imshow('test', canvas)
    # cv2.waitKey(0)
    # cv2.destroyAllWindows()
